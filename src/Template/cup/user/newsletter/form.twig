{% extends 'cup/layout.twig' %}

{% block breadcrumb %}
    <div class="page-inner py-5">
        <div class="d-flex align-items-left align-items-md-center flex-column flex-md-row">
            <div>
                <h2 class="text-white pb-2 fw-bold">{{ 'New newsletter'|locale }}</h2>
            </div>
        </div>
    </div>
{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <form method="post">
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                {% include 'cup/form.twig' with {
                                    'label': 'Title'|locale,
                                    'type': 'text',
                                    'name': 'subject',
                                    'postfix': '<i class="far fa-question-circle" data-toggle="tooltip" data-placement="top" title="Название письма"></i>',
                                    'args': {
                                        'required': true,
                                    }
                                } %}
                            </div>
                            <div class="col-md-6">
                                {% include 'cup/form.twig' with {
                                    'label': 'Recipients'|locale,
                                    'type': 'select',
                                    'name': 'type',
                                    'args': {
                                        'option': _(reference('/App/Domain/References/User::NEWSLETTER_TYPE')),
                                        'selected': reference('/App/Domain/References/User::NEWSLETTER_TYPE_ALL')
                                    }
                                } %}
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-12">
                                {% include 'cup/editor.twig' with {
                                    'label': 'Message'|locale,
                                    'name': 'body'
                                } %}
                            </div>
                        </div>
                    </div>

                    <div class="card-action">
                        <button type="submit" name="save" value="exit" class="btn btn-primary">
                            {{ 'Submit'|locale }}
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
{% endblock %}
