{% extends 'cup/layout.twig' %}

{% block breadcrumb %}
    <div class="page-inner py-5">
        <div class="d-flex align-items-left align-items-md-center flex-column flex-md-row">
            <div>
                <h2 class="text-white pb-2 fw-bold">{{ 'List of files'|locale }} ({{ list.count() }})</h2>
            </div>
        </div>
    </div>
{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-body">
                    {% embed 'mixin/datatable.twig' %}
                        {% block thead %}
                            <tr>
                                <th>{{ 'File name'|locale }}</th>
                                <th width="15%">{{ 'Size'|locale }}</th>
                                <th width="15%" class="d-none d-md-table-cell">{{ 'Creation date'|locale }}</th>
                                <th width="20%" class="text-right"></th>
                            </tr>
                        {% endblock %}

                        {% block tbody %}
                            {% for item in list.sortByDesc('date') %}
                                <tr>
                                    <td>
                                        <a href="{{ item.getPublicPath() }}" target="_blank">
                                            {{ item.getFileName()|u.truncate(50, '...') }}
                                            <i class="fas fa-external-link-alt"></i>
                                        </a>
                                    </td>
                                    <td width="15%">{{ item.getFileSize() }}</td>
                                    <td width="15%" class="d-none d-md-table-cell">{{ item.date|df }}</td>
                                    <td width="20%" class="text-right">
                                        <form action="/cup/file/{{ item.uuid }}/delete" style="display: inline-block" method="post" onsubmit="return confirm('{{ 'Are you sure you want to delete the file?'|locale }}');">
                                            <div class="btn-group">
                                                <button type="submit" class="btn btn-sm btn-danger">
                                                    <i class="fas fa-fw fa-trash-alt"></i>
                                                    <span class="d-none d-md-inline-block">{{ 'Delete'|locale }}</span>
                                                </button>
                                            </div>
                                        </form>
                                    </td>
                                </tr>
                            {% endfor %}
                        {% endblock %}
                    {% endembed %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
