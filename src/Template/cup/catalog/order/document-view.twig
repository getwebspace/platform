{% extends 'cup/layout.twig' %}

{% block title %}{{ document.title|locale }} #{{ order.external_id ?: order.serial }}{% endblock %}

{% block breadcrumb %}
    <div class="page-inner py-5">
        <div class="d-flex align-items-left align-items-md-center flex-column flex-md-row">
            <div>
                <h2 class="text-white pb-2 fw-bold">{{ document.title|locale }}</h2>
            </div>
            <div class="ml-md-auto py-2 py-md-0">
                <a href="/cup/catalog/order/{{ order.uuid }}/edit" class="btn btn-secondary">
                    {{ 'Edit order'|locale }}
                </a>
                <a href="#" onclick="print_element('#invoice')" class="btn btn-light">
                    {{ 'Print'|locale }}
                </a>
            </div>
        </div>
    </div>
{% endblock %}

{% block content %}
    <div id="invoice" class="card">
        {{ include(template_from_string(template)) }}
    </div>
{% endblock %}
