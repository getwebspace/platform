<!DOCTYPE html>
<html lang="{{ parameter('common_lang', 'ru') }}">
    <!-- OpenSource WebSpace Engine CMS (Full free for all) -->
    <!-- GitHub: https://github.com/getwebspace -->

    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="generator" content="OpenSource WebSpace Engine CMS" />

        {% block head '' %}
        {% block style '' %}

        <title>{% block title deferred %}{{ title ? title ~ ' | ' : '' }}{{ parameter('common_title') }}{% endblock %}</title>
    </head>

    {% block body %}
        <body>
            {% block bodyinner '' %}
            {% block script '' %}
        </body>
    {% endblock %}
</html>
